#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int main() {
    // Write your code here

    // int a = 5;

    // int *ptr_a_1 = &a ;
    // int *ptr_a_2 = &a ;

    // cout<< "A : " << a << endl;
    // cout << "Pointer 1 to A : " << ptr_a_1 << endl;
    // cout << "Pointer 2 to A : " << ptr_a_2 << endl;

    // cout << "Address of Pointer 1 to A : " << *ptr_a_1 << endl;
    // cout << "Address of Pointer 2 to A : " << *ptr_a_2 << endl;

    // cout << "Dereferenced Pointer 1 to A : " << &ptr_a_1 << endl;
    // cout << "Dereferenced Pointer 2 to A : " << &ptr_a_2 << endl;

    // cout << "Dereferenced Address of Pointer 1 to A : " << *(&ptr_a_1) << endl;
    // cout << "Dereferenced Address of Pointer 2 to A : " << *(&ptr_a_2) << endl;

    // (*ptr_a_1)++;
    // cout << (*ptr_a_1) << endl;
    // cout << a ;

    // char str[] = "abcde" ;
    // char *ptr = &str[0];
    // cout<<ptr[0]<<endl;
    // cout<<str;

    // char a = 'a';
    // char *ptr = &a;
    // cout<<"a: " << a << " ptr->a: "<<ptr<<endl;
    // cout << "Size of character variable a: "<<sizeof(a) << endl;
    // cout << "Size of pointer to character variable a: "<< sizeof(ptr) << endl;
    // cout << "Size of pointer to character variable a's 0th index: "<< sizeof(ptr[0]) << endl;

    // int i=5;
    // int *ptr =&i;
    // int **ptr_2 = &ptr;

    // cout<< "i: "<<i<<endl;
    // cout<< "ptr: "<<ptr<<endl;
    // cout<< "ptr_2: "<<ptr_2<<endl;

    // cout<<"Dereferencing ptr: "<<*ptr<<endl;
    // cout<<"Dereferencing ptr_2: "<<*ptr_2<<endl;
    // cout<<"Dereferencing ptr_2 twice: "<<**ptr_2<<endl;

    // int first =6;
    // int *p = &first;
    // int *q = p;

    // int *p = 0;
    // int first = 110;
    // *p = first;
    // cout<<*p;

    // int first = 8, second = 11;
    // int *third = &second;

    // first = *third; // => first = *(&second) => first = second
    // // first = second = 11

    // *third = *third + 2 ; // => *(&second) = *(&second) + 1 => second += 2
    // // i.e second = 13

    return 0;
}